Machine code debugging so far:

b4 --> mov ah,
b0 --> mov al,
cd --> interrupt handler
55 aa --> magic number


----------------------------------------------------------------------------------

CHAPTER 2:

nasm line: nasm (asm file) -f bin -o (bin file name) 
setup:

times 510-($-$$) db 0
dw 0xaa55

(magic number to indicate that this is a bootloader)

----------------------------------------------------------------------------------

CHAPTER 3

Most of the operations are done in real mode. (16 bit mode).

3.3.1 --> Interrupts
Interrupts are mechanisms that forces the CPU to halt and do some operations. The one
we're using in CHAPTER 3 is the 0x10 (write to display) interrupt, and the 0x13
(I/o interrupt service routine) interrupt are some that exist. 

3.3.2 --> CPU registers
CPU registers have 4 general purpose:
ax, bx, cx, and dx. these are general purpose and can be used for everything.

ALL follow format of:

mov (register), (value)
mov ax, 1234
mov cx, 0x234
mov dx, 't'
mov bx, ax

These registers are 'words' though (2 bytes), so we can divide to the high
and low byte.

mov ax , 0 ; ax -> 0 x0000 , or in binary 0000000000000000
mov ah , 0 x56 ; ax -> 0 x5600
mov al , 0 x23 ; ax -> 0 x5623
mov ah , 0 x16 ; ax -> 0 x1623

3.3.3 --> putting it all together

jmp $ infinitely jumps to itself
jmp loop jumps to loops

3.4.2 --> "X" marks the spot

Based on the code it wouldn't fucing work but here are some highlights:

label:
	db (define byte)

org 0x7c00 (organize 0x7c00 as base for all operations)
xor (excluded or) itself to initalize
ds (data segment): register that tells CPU WHERE DATA LIVES IN MEMORY

3.4.3 --> Defining strings

(lowk like a trick but)

defining strings is like:

db 'Booting OS', 0

but only defines the first byte.

This is how you actually print a string:

org 0x7C00		;still organizing

xor ax, ax		;still initializing
mov ds, ax		;same

mov ah, 0x0E		;still teletyping

mov si, the_string   ; SI = pointer to string THIS IS THE IMPORTANT ONE.

print_loop:
    mov al, [si]     ; load current character
    cmp al, 0        ; is it the end? We zero indexed it, if you didn't know.
    je done			 ; if equal move to done.

    int 0x10         ; print it

    inc si           ; move to next character
    jmp print_loop   ; repeat the print_loop

done:
    jmp $

the_string:
    db 'Booting OS', 0

times 510-($-$$) db 0
dw 0xAA55

yeah loops are sick

3.4.4 --> Using the stack

There is 1 stack in the CPU. It uses push and pop.
in 16 bit mode, you can only push and pop 16-bit boundaries
cpu regiters bp and sp, stack base and stack top.
stack grows downwards from base pointer, and sp is decreased by value's size.

funny bc i learned that cx is the loop counter for loop function
also i learned that its best to put stack pointers at 0x8000 so no overwrite

3.4.5 --> control structures

je target ; jump if equal ( i.e. x == y )
jne target ; jump if not equal ( i.e. x != y )
jl target ; jump if less than ( i.e. x < y )
jle target ; jump if less than or equal ( i.e. x <= y )
jg target ; jump if greater than ( i.e. x > y )
jge target ; jump if greater than or equal ( i.e. x >= y )

simple.

3.4.6 --> calling functions

"call", "return" for simpler code
"pusha", "popa" push all and pop all registers to stack

3.4.7 --> include files

%include "lasdknlaknsd.asm" to include files. not that hard

3.5.1 --> complete implementation of print_hex function.
AND --> bitwise and,
SHR --> bitwise shift
i dont know i pretty much failed lmao

3-6-1: Extended Memory Access using Segments
All we're writing in our bootloader is first 512 bytes. Using BIOS, we allow ourself to manipulate data on the drives.

when the CPU runs in 16 bit mode  --> 0xFFFF max address:
64 KB total. Too small, so added registers

"cs" "ds" "ss" "es", segment registers

In essence, these are our OFFSETS, our DEFAULTS that our
CPU can now access from 0x0000 to 0xffff. For example,
if we mov ax, [0x45ef] instead of actual 0x45ef we would
mov offset (data segment, ds) + 0x45ef. Similarly, the
stack segment (ss) is used to offset the stack's base
pointer, bs.

Different segments could overlap.

HOW OFFSETS WORK:
CPU multiplies value in segment register by 16, adds offset

eg: ds = 0x4d, CPU: 0x4d * 16 = 0x4d0
mov ax, 0x001 ==> 0x4d0 + 0x001 = 0x4d1.

ORG instead just makes entire math by 0x7c00, not something we want.

CANNOT SET SEGMENT POINTERS DIRECTLY, USE EXISTING REGISTERS TO MODIFY

mov bx, 0x7c0
mov ds, bx                  ;ds is now 0x7c0.
mov al, [the_secret]
int 0x10

the_secret:
    db "X"

will print it.

3-6-2 --> How disk drives work
just cylinders, heads, sectors of a hard disk. irrelevant today ig

The sector is 512 bytes, then it's the head, then the cylinders

3-6-3 --> using the BIOS to read the disk.

the BIOS routine we use is calling int 0x13 after setting register
al to 0x02.

We now need to set "dl" (what drive), "ch" (cylinder), "dh" (track?), "cl" (the sector), and "al" (read al sectors from start point)

its so fucking confusing

Also, BIOS expects it to read it to ES:BX for some fucking reason, so
general-purpose segment ES with offset BX

mov bx, 0xa000
mov es, bx
mov bx, 0x1234

Now the data will be read to 0xa000:0x1234, which CPU will translate to 0xa1234 (bc 0xa000 * 16 + 0x1234 = 0xa1234)

now int 0x13. BIOS will now interrupt and actually read this shit.

Entire thing is kinda bull but it works ig

oh and also jc means "jump if carry flag"


there is a carry flag function called "disk_error"

oh and by the way I could just do:

msg db 'hello, world!' ,0 and move msg to si for simplicity

It is at this point where I will write a functioning bootloader

I also learned that 0x08 is move cursor back
ax 0003h is clear screen, int 10h is do everything
MATCH THE SECTOR NUMBERS WITH THE AMOUNT OF SECTORS IN STANDARDLIBRARY
Teletype 0x0e works well enough for printing

-------------------------------------------------------------------------------
CHAPTER 4: 32BIT

4.1 --> Adapting to life without BIOS.
No more int 0x10 for text writing, and no more teletype.
I need to manually append text to address 0xb8000 (VGA output mode)

VGA MEMORY ADDRESS CODE:
0xb8000 + 2 * (row * 80 + col)
I don't know why but it works.
*2 for color, text dividor,
80 characters per row and column as an individual unit of base is probably why

We have color now!

text can be set with edx, mov it to mvideo_memory by
mov edx, 0xb8000 (simplicity)

mov al to the CONTNET of ebx if we assume that ebx has our string content
(mov al, [ebx])
and set color:
(mov ah, 0x0f)

cmp al, 0
je done

mov [edx], ax
add ebx, 1 (increment ebx to next character)
add edx, 2 (increment to next character cell: color AND text)

4.2 --> Understanding GDT

I guess gdt is a method of forcing a bunch of requirements onto the CPU so that it can reach 32bit.

mov bx , 0 x4000
mov es , bx
mov [ es :0 xfe56 ] , ax

Although the general idea of segmenting memory and using offsets to reach into those
segments has remained the same, the way that it is implented in protected mode has
completely changed, primarily to afford more flexibility. Once the CPU has been switched
into 32-bit protected mode, the process by which it translates logical addresses (i.e.
the combination of a segment register and an offset) to physical address is completely
different: rather than multiply the value of a segment register by 16 and then add to it
the offset, a segment register becomes an index to a particular segment descriptor (SD)
in the GDT.
A segment descriptor is an 8-byte structure that defines the following properties of
a protected-mode segment:
• Base address (32 bits), which defines where the segment begins in physical memory
• Segment Limit (20 bits), which defines the size of the segment
• Various flags, which affect how the CPU interprets the segment, such as the
privilige level of code that runs within it or whether it is read- or write-only.

yeah this shit is confusing. I know just normal segment-based addressing but the rest is really confusing

I haveto follow the "basic flat model" which makes life easier ig

Honestly seems like the solutions to my problems.

Why fix something when it aint broke?

; GDT
gdt_start :
gdt_null : ; the mandatory null descriptor
dd 0 x0 ; ’ dd ’ means define double word ( i.e. 4 bytes )
dd 0 x0
gdt_code : ; the code segment descriptor
; base =0 x0 , limit =0 xfffff ,
; 1 st flags : ( present )1 ( privilege )00 ( descriptor type )1 -> 1001 b
; type flags : ( code )1 ( conforming )0 ( readable )1 ( accessed )0 -> 1010 b
; 2 nd flags : ( granularity )1 (32 - bit default )1 (64 - bit seg )0 ( AVL )0 -> 1100 b
dw 0 xffff ; Limit ( bits 0 -15)
dw 0 x0 ; Base ( bits 0 -15)
db 0 x0 ; Base ( bits 16 -23)
db 10011010 b ; 1 st flags , type flags
db 11001111 b ; 2 nd flags , Limit ( bits 16 -19)
db 0 x0 ; Base ( bits 24 -31)
gdt_data : ; the data segment descriptor
; Same as code segment except for the type flags :
; type flags : ( code )0 ( expand down )0 ( writable )1 ( accessed )0 -> 0010 b
dw 0 xffff ; Limit ( bits 0 -15)
dw 0 x0 ; Base ( bits 0 -15)
db 0 x0 ; Base ( bits 16 -23)
db 10010010 b ; 1 st flags , type flags
db 11001111 b ; 2 nd flags , Limit ( bits 16 -19)
db 0 x0 ; Base ( bits 24 -31)
gdt_end : ; The reason for putting a label at the end of the
; GDT is so we can have the assembler calculate
; the size of the GDT for the GDT decriptor ( below )
; GDT descriptior
gdt_descriptor :
dw gdt_end - gdt_start - 1 ; Size of our GDT , always less one
; of the true size
dd gdt_start ; Start address of our GDT
; Define some handy constants for the GDT segment descriptor offsets , which
; are what segment registers must contain when in protected mode. For example ,
; when we set DS = 0 x10 in PM , the CPU knows that we mean it to use the
; segment described at offset 0 x10 ( i.e. 16 bytes ) in our GDT , which in our
; case is the DATA segment (0 x0 -> NULL ; 0 x08 -> CODE ; 0 x10 -> DATA )
CODE_SEG equ gdt_code - gdt_start
DATA_SEG equ gdt_data - gdt_start